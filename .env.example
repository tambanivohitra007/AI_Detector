# OpenAI Configuration
OPENAI_API_KEY=your-actual-api-key-here

# Server Configuration
PORT=3000
NODE_ENV=development

# Authentication — admin credentials (optional if Microsoft OAuth is configured)
AUTH_USERNAME=admin
AUTH_PASSWORD=change-me

# Microsoft OAuth (optional — set all three to enable "Sign in with Microsoft")
# MICROSOFT_CLIENT_ID=your-azure-app-client-id
# MICROSOFT_CLIENT_SECRET=your-azure-app-client-secret
# MICROSOFT_REDIRECT_URI=http://localhost:3000/auth/microsoft/callback
# MICROSOFT_TENANT=organizations
# ALLOWED_EMAIL_DOMAIN=apiu.edu

# CORS Configuration (optional - set to your domain in production)
ALLOWED_ORIGINS=*

# Session (optional)
# SESSION_EXPIRY_MS=86400000

# Request Signing (optional - auto-generated if not set)
# SIGNING_SECRET=your-random-secret-here
# TOKEN_EXPIRY_MS=900000
